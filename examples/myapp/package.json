{
  "name": "myapp",
  "version": "0.0.1",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc --build",
    "docker-build": "docker build --tag myapp:latest .",
    "docker-run": "docker run --init --rm myapp:latest"
  },
  "author": "Cory Sherman",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@tsconfig/node14": "^1.0.0",
    "@types/express": "^4.17.8",
    "typescript": "^4.0.5"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
